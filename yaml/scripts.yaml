# scripts.yaml
api_radio_script:
  alias: "API radio script"
  mode: single
  fields:
    param:
      description: "URL de streaming"
  sequence:
    - service: rest_command.karotz_play_radio
      data:
        karotzStreamUrl: "{{ param }}"